# **平台开发宪法与核心架构原则 (V1.0)**

**文档目的**: 本文档是项目的最高指导原则，旨在记录平台的核心设计理念、架构决策和开发“铁律”。所有后续的开发、重构和功能添加，都必须严格遵守本文档中定义的规则，以确保项目的一致性、稳定性和可维护性。

---

## **第一条：UI与导航的稳定性原则**

1.  **导航结构神圣不可侵犯**:
    *   **左侧主导航栏是唯一的功能入口**: 所有一级功能模块的入口都必须在左侧主导航栏中明确列出。
    *   **禁止随意变更**: 除非得到项目所有者的明确许可，严禁擅自增加、删除、合并或调整导航栏中任何项目的顺序、名称或层级。导航栏的结构和布局具有最高优先级。

2.  **统一的管理后台入口**:
    *   所有面向管理员的后台管理功能，必须被统一整合到“管理后台”(`Admin Dashboard`)模块下。
    *   左侧主导航栏中只保留一个指向 `/admin-dashboard` 的“管理后台”入口。所有细分的管理页面（如LLM对接、权限管理、提示词管理等）都应通过此统一入口进行访问。

---

## **第二条：数据资产的核心定义与分离**

平台拥有三大核心、分离的数据资产库，各自有明确的定位和用途：

1.  **供应商和商品服务库 (`suppliers`, `products` 集合)**:
    *   **定位**: 平台的核心交易资产。
    *   **内容**: 存储所有供应商的公司信息、资质以及他们提供的具体产品和服务的结构化数据（名称、价格、规格等）。这是构成平台可交易商品的基础。

2.  **知识库 (`products` 集合的延伸)**:
    *   **定位**: 结构化的内部知识中心。
    *   **内容**: 它在 `products` 集合的基础上，通过手动录入、批量导入等方式，构建一个丰富、详尽、结构化的产品和服务信息库。其目的是为AI提供精准、可靠的、关于“我们有什么”的数据。

3.  **公共资源库 (`resources` 集合)**:
    *   **定位**: 外部行业资讯与情报中心。
    *   **内容**: 专门用于收集、整理、分类和标记**外部的、非结构化的行业资讯和动态**（例如：科技新闻、市场趋势、设计灵感等）。其核心价值在于为AI提供关于“外界正在发生什么”的宏观背景知识，而不是管理技术性资产（如API Key）。

---

## **第三条：平台智能化的核心驱动飞轮**

平台的AI能力由一个清晰、单向的数据流驱动，此为“核心飞轮”原则：

**数据 (Data) -> 提示词 (Prompt) -> 场景 (Scenario) -> 体验 (Experience)**

*   **数据 (Data)**: 所有AI分析的原材料，唯一来源是我们自己管理的 **三大核心数据资产库** (供应商/商品库、知识库、公共资源库)。
*   **提示词 (Prompt)**: 数据的使用说明书。一个提示词定义了如何组织和利用一个或多个数据源的数据，去完成一个特定的AI任务（如“生成用户画像”或“推荐商品”）。提示词是受保护的知识产权。
*   **场景 (Scenario)**: AI能力与业务逻辑的连接器。一个场景（如“聊天AI助理”）定义了一个具体的业务环节，并将其绑定到一个或多个提示词上。运营人员可以通过后台配置，动态切换场景所使用的提示词，而无需修改任何业务代码。
*   **体验 (Experience)**: AI能力的最终呈现。用户在平台的各个角落（如AI导购、需求匹配等）与AI的交互，其背后都是由一个具体的“场景”配置所驱动的。

**所有AI相关的开发都必须遵循此流程。** 任何AI功能的实现，都必须思考它属于哪个场景、需要调用哪个提示词、以及这个提示词需要从哪个核心数据资产库中获取数据。

---

## **第四条：API密钥的统一管理原则**

1.  **集中配置**: 所有第三方服务的API Key（例如 OpenAI, Tripo3D, Google AI 等），都**必须**在“管理后台”的“LLM对接”(`llm_connections` 集合)模块中进行统一配置和管理。
2.  **严禁他处存储**: 严禁将任何API Key硬编码在代码中、存储在 `resources` 集合或任何其他数据库集合中。`llm_connections` 是API Key的唯一合法存储位置。
3.  **调用透明化**: 上层业务逻辑在调用AI功能时，不应直接关心API Key。AI流程（Flow）应在内部自行从`llm_connections`中获取所需密钥。

---
**本文档是项目的“宪法”，所有团队成员（包括AI）都必须严格遵守。**

# TCB 快速部署指南 - 修复静态资源 404

## 📦 部署包信息

| 项目 | 说明 |
|------|------|
| **文件名** | `leverage-deployment-fixed.zip` |
| **大小** | 19.25 MB |
| **生成时间** | 2025-10-27 22:34 |
| **包含内容** | 完整的 Next.js standalone 部署文件 + 所有静态资源 |

## 🚀 部署步骤（5分钟完成）

### 步骤 1：登录 TCB 控制台
```
访问: https://console.cloud.tencent.com/tcb
进入: 你的应用 (leverage03-194704)
```

### 步骤 2：清空旧部署文件
1. 进入 **CloudBase 托管** → **文件管理**
2. 删除以下目录：
   - `/.next` （完全删除）
   - `/node_modules` （完全删除，重新上传会包含）
   - `/public` （可选）

3. 点击 **删除** 确认

### 步骤 3：上传新部署包
#### 方式 A - 通过 Web 界面（推荐新手）
1. 在文件管理中点击 **上传文件夹** 或 **上传**
2. 选择本地 `.deploy/` 目录（或解压 ZIP 后的文件）
3. 将以下文件夹/文件上传到根目录：
   ```
   上传到 TCB 根目录 /
   ├── .env
   ├── .next/                    ← 关键：包含 static 目录
   ├── data/
   ├── node_modules/
   ├── public/
   ├── package.json
   ├── server.js
   └── 其他文件
   ```

#### 方式 B - 使用 ZIP（如果支持）
1. 在文件管理中上传 `leverage-deployment-fixed.zip`
2. 在 TCB 控制台执行解压命令（通常自动解压）
3. 验证文件是否正确解压

### 步骤 4：验证部署

完成上传后，检查以下文件是否存在于 TCB 文件管理：

```
✅ /.next/static/             ← 最关键！
✅ /.next/static/chunks/      ← 必须包含 .js 文件
✅ /server.js
✅ /package.json
```

### 步骤 5：重启应用

1. 进入 **应用基本信息** 或 **云托管服务**
2. 点击 **重启** 或 **重新部署**
3. 等待应用启动（通常 30-60 秒）

## ✅ 部署验收清单

访问你的应用 URL（`https://leverage03-194704-4-1382937545.sh.run.tcloudbase.com/`），按顺序检查：

### 页面加载
- [ ] 主页能加载（无白屏）
- [ ] 浏览器标签页显示标题（非"应用错误"）
- [ ] 页面 HTML 能看到（不是错误消息）

### 浏览器控制台检查
打开浏览器开发者工具（F12）→ **控制台**，检查：

- [ ] 无 404 错误（特别是 `_next/static/chunks/*.js`）
- [ ] 无红色错误信息
- [ ] 可能有黄色警告（正常）

**快速验证命令**（在控制台粘贴）：
```javascript
// 验证 webpack chunk 是否加载
fetch('/_next/static/chunks/')
  .then(r => console.log('✅ 静态资源可访问', r.status))
  .catch(e => console.error('❌ 静态资源失败', e));
```

### 功能检查
- [ ] 页面显示"注册"或"登录"按钮
- [ ] 可以点击按钮跳转（不卡顿）
- [ ] 表单字段显示正确

### 可选：功能测试
- [ ] 注册新用户（输入手机号）
- [ ] 接收短信验证码
- [ ] 登录成功
- [ ] 访问 `/admin-manager` 页面（需管理员权限）

## 🔧 故障排查

### 问题 1：仍然显示 404 或"应用错误"

**原因**：文件可能未完全上传或没有刷新缓存

**解决**：
1. 清空浏览器缓存：`Ctrl+Shift+Delete` 或使用隐身窗口
2. 在 TCB 控制台清除 CDN 缓存（如启用）
3. 重新上传部署文件
4. 等待 60 秒后重新访问

### 问题 2：某些 chunk 文件还是 404

**原因**：`.next/static/chunks/` 目录上传不完整

**解决**：
1. 在 TCB 文件管理中检查 `/.next/static/chunks/` 是否存在
2. 如果缺失，手动上传本地 `.deploy/.next/static/chunks/` 下的所有文件
3. 刷新页面

### 问题 3：短信功能不工作

**原因**：可能是环境变量问题（TCB_ENV_ID、TCB_SECRET_ID 等）

**解决**：
1. 检查 `.env` 文件是否已上传到根目录
2. 确认 `.env` 包含必要的 TCB 配置
3. 重启应用后重试

### 问题 4：应用启动但页面加载很慢

**原因**：可能是 node_modules 或第一次启动

**解决**：
1. 稍等 1-2 分钟（应用首次启动会安装依赖）
2. 刷新页面
3. 检查 TCB 日志（如可用）

## 📋 部署包内容详解

```
leverage-deployment-fixed.zip (19.25 MB)
└── .deploy/
    ├── .env                       # 环境变量配置
    ├── .next/
    │   ├── server/               # Next.js 服务端代码
    │   ├── static/               # ✅ 客户端静态资源（最关键）
    │   │   ├── chunks/           # JavaScript bundle
    │   │   ├── css/              # 样式文件
    │   │   └── ...
    │   ├── app-build-manifest.json
    │   ├── BUILD_ID
    │   ├── package.json
    │   └── 其他配置文件
    ├── data/                      # 应用数据目录
    ├── node_modules/              # npm 依赖（~200MB+）
    ├── public/                    # 公共文件（logo、manifest 等）
    ├── package.json               # npm 包定义
    ├── server.js                  # 应用入口点
    └── ...
```

## 📞 需要帮助？

如果部署后仍有问题：

1. **检查文件大小**：部署包应该是 ~19-30 MB
2. **检查文件完整性**：特别是 `.next/static/` 目录
3. **查看应用日志**：TCB 控制台通常提供实时日志
4. **尝试本地测试**：在本地运行 `.deploy/server.js` 验证配置

---

**最后更新**：2025-10-27 22:34
**下次部署**：如需更新，重复以上步骤（删除旧文件 → 上传新文件 → 重启应用）

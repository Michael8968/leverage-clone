'use client';

/**
 * A purely CSS-driven background component that creates a subtle,
 * rotating hologram effect. It's designed to be lightweight and
 * performant, avoiding any JavaScript library dependencies.
 */
export function HologramBackground() {
  return (
    <div className="hologram-background fixed inset-0 -z-10">
    </div>
  );
}

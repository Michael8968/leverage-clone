# **AI 智能匹配与创意生成平台 - 产品需求文档 (PRD)**

**版本**: 2.3 (Final)
**日期**: 2024年8月10日
**状态**: 已完成

---

## **1. 愿景与战略**

### **1.1. 产品愿景**
构建一个以AI为核心的智能化商业生态系统，无缝连接用户的个性化、模糊化需求与高品质、可定制的产品及服务。我们致力于成为创意和需求的最佳“翻译官”和“匹配者”，让每一次发现都充满惊喜，让每一次交易都精准高效。

### **1.2. 核心问题**
*   **用户侧**: 消费者常常“只可意会，不可言传”，难以通过关键词找到满意的、独特的商品。
*   **供给侧**: 优质供应商和独立设计师，其独特的产品和服务难以精准触达有潜在需求的客户。

### **1.3. 战略目标 (Success Metrics)**
*   **提升匹配成功率**: AI推荐的商品首轮点击率 > 40%。
*   **促进供需撮合**: “需求池”中的需求在72小时内的响应率 > 60%。
*   **激活创意生态**: “创意者工作台”月活跃创作者数量持续增长。
*   **平台数据增值**: 知识库（产品/服务/提示词/场景配置）条目持续增长，形成数据飞轮效应。

---

## **2. 用户画像 (Personas)**

#### **2.1. 管理员 - 李明 (Admin)**
*   **背景**: 平台运营总监，技术背景，追求效率和数据驱动。
*   **目标**: 保证平台AI模型的精准度和数据质量；监控平台健康度；高效管理所有用户和资源。
*   **痛点**: AI推荐效果波动大，难以归因；各模块数据分散，难以统一管理；用户权限分配和管理操作繁琐。
*   **核心诉求**: 一个强大、中心化的后台，能够精细化控制AI行为（LLM模型、提示词、场景策略）、管理核心数据（产品、需求），并对用户进行生命周期管理。

#### **2.2. 供应商 - 创新科技 (Supplier)**
*   **背景**: 一家专注于智能家居产品的科技公司，拥有高质量的产品线但线上推广渠道有限。
*   **目标**: 寻找新的、精准的销售渠道；接收定制化订单以提升品牌价值。
*   **痛点**: 传统广告投放费用高昂且效果不佳。
*   **核心诉求**: 一个能“听懂”客户潜在需求的平台，将订单直接推送到面前；一个简单易用的后台来管理自己的产品和服务信息。

#### **2.3. 创意者 - 王芳 (Creator)**
*   **背景**: 自由职业3D设计师，对新潮事物充满热情，希望将创意变现。
*   **目标**: 找到能发挥自己设计才能的兼职项目；使用高效的工具进行创作；建立个人品牌。
*   **痛点**: 独立接单不稳定；需要AI工具辅助创作灵感。
*   **核心诉求**: 一个能稳定提供真实设计任务的“任务市场”；一个能将文本描述快速转化为3D模型参考的AI创作工具；一个可以管理和复用自己提示词的工具库。

#### **2.4. 普通用户 - 张伟 (User)**
*   **背景**: 追求生活品质的都市白领，喜欢为朋友和自己挑选独特的礼物。
*   **目标**: 快速找到符合自己模糊想法的商品；能够进行高端、个性化的定制。
*   **痛点**: 传统电商搜索无法满足其感性、模糊的搜索需求。
*   **核心诉求**: 一个能理解自然语言甚至图片的“导购AI”；一个在AI推荐不满意时，能将需求公开发布的渠道。

---

## **3. 功能规格 (Functional Specifications)**

### **3.1. 主页: AI 购物助手 (`/dashboard`)**

*   **用户故事**: 作为一个**普通用户**，我希望能通过输入文字或上传图片来描述我的想法，平台能理解我的需求并从**真实的产品库**中推荐相关商品。
*   **核心功能**:
    1.  **智能聊天界面**: 支持文本和图片混合输入。
    2.  **AI分析与响应**: 依次调用 `generateUserProfile` 和 `getProductRecommendations` 流程，返回包含“用户画像”和“商品推荐”的AI消息。
    3.  **快捷操作入口**: 提供“高端定制”和“发布到需求池”的入口。

### **3.2. 需求池 (`/demand-pool`)**

*   **用户故事**: 作为一个**管理员**，我希望能为公开的需求，调用AI从真实的产品/供应商库中寻找最佳匹配。作为一个**创意者**，我能在此“抢单”并与需求方沟通。
*   **核心功能**:
    1.  **需求列表与发布**: 展示所有需求，并允许用户发布新需求。
    2.  **AI匹配推荐 (管理员功能)**: 调用`recommendCreatives` AI流程，为选中的需求匹配创意方。
    3.  **抢单与沟通**: `creator`或`supplier`可以“抢单”；相关方可以通过`ChatDialog`进行实时沟通，并可启用“AI助理”辅助交流。

### **3.3. 创意者工作台 (`/creator-workbench`)**

*   **用户故事**: 作为一个**创意者**，我希望能在一个地方看到所有我可以接的真实任务，并使用多个AI工具进行创作，还能方便地查看我的提交历史。
*   **核心功能**:
    1.  **任务与需求 (Tab)**: 从`demands`集合加载所有“开放中”的真实需求。
    2.  **AI 创作 (Tab)**: 集成多个AI图像生成工具，调用相应AI流程生成预览图，并通过`SubmissionForm`将作品存入`products`集合进行审核。
    3.  **我的提交 (Tab)**: 从`products`集合加载**当前创作者**已提交的所有作品和审核状态。

### **3.4. 管理后台 (`/admin-dashboard`) (Admin-Only)**

*   **用户故事**: 作为一个**管理员**，我需要一个独立的后台来集中管理平台所有的大语言模型（LLM）API连接，以确保AI功能的稳定和可扩展。
*   **核心功能**:
    1.  **LLM连接列表**: 集中展示所有已配置的LLM模型连接，包含供应商、模型名称、优先级和状态等关键信息。
    2.  **添加/编辑LLM连接**:
        *   **[UI]** 提供统一的`LlmConnectionForm`表单。
        *   **[Usability]** “厂商”、“模型名称”等字段采用“选填+输入”的`Combobox`组件，既提供主流模型的预设选项，也支持直接输入自定义值。
    3.  **连接健康检查**:
        *   **[Action]** 提供“可用性测试”按钮。
        *   **[Flow]** 点击后触发`testLlmConnection`后端流程，该流程通过统一的`executePrompt` API网关向目标模型发送真实请求。
        *   **[Feedback]** 通过`Toast`组件实时向管理员反馈连接是否成功。

### **3.5. 提示词管理 (`/prompt-management`) (Admin/Creator)**

*   **用户故事**: 作为一个**管理员**，我需要管理平台所有的提示词，并能将其与特定的LLM模型绑定。作为一个**创意者**，我希望能创建和管理我自己的提示词，并调用平台的通用提示词，但不能看到其内容。
*   **核心功能**:
    1.  **提示词列表**: 展示用户有权查看的提示词，包含名称、唯一的`promptKey`、状态和绑定的执行模型。
    2.  **知识产权保护**:
        *   **[Logic]** 通过`promptKey`机制实现调用。用户只能通过`promptKey`使用提示词，而无法看到其具体内容（除非是所有者或管理员）。
    3.  **新增/编辑提示词 (`PromptEditDialog`)**:
        *   **[UI]** 弹窗中提供表单，用于编辑提示词的名称、描述、内容等。
        *   **[Data]** 新增提示词时，系统会自动生成一个唯一的`promptKey`。
        *   **[Feature]** 提供“调用地址(Base URL)”的显示和复制功能，方便外部集成。
        *   **[Feature]** 提供“一键导入元提示词”功能，快速填充专业模板。
    4.  **模型绑定**:
        *   **[UI]** 表单中提供“绑定模型”下拉框，选项为所有“活跃”的LLM连接。
        *   **[Data]** 允许将一个提示词与一个具体的`llm_connections`文档进行关联，并设置调用优先级。
    5.  **查询范围与温度配置**: 允许在编辑提示词时，精确指定该提示词可以从哪些核心数据源（如知识库、供应商库）中检索信息，并为每个数据源独立设置创造性“温度”。
        
### **3.6. AI场景配置 (`/ai-scenario-config`) (Admin-Only) (新增)**

*   **用户故事**: 作为一个**管理员**，我希望能在一个中心化的界面，为平台中不同的业务场景（如“聊天助理”）配置其背后驱动的AI行为，并且可以设定这些配置在特定的**时间**、对特定的**用户群体**生效，实现精细化的A/B测试和运营。
*   **核心功能**:
    1.  **场景列表**: 以表格形式列出所有平台内预定义的、可配置的AI业务场景。
    2.  **配置绑定**: 允许管理员为每个场景绑定一个已创建的“提示词 (Prompt)”。
    3.  **高级规则配置 (`ScenarioEditDialog`)**:
        *   **[UI]** 提供一个功能强大的编辑弹窗，使用折叠面板 (`Accordion`) 来组织复杂的配置项。
        *   **时间维度**: 
            *   **绝对时间**: 支持配置一次性的“生效时间”和“失效时间”。
            *   **重复策略**: 支持配置“按天”或“按周”重复。当选择“按周”时，可勾选每周的特定几天（如周一、三、五）。
            *   **时间窗口**: 支持配置在重复周期内生效的“开始时间”和“结束时间”（如 08:00 - 20:00）。
        *   **用户维度**:
            *   **角色限定**: 支持勾选一个或多个用户角色（`admin`, `creator`等）。
            *   **星级限定**: 当勾选某个角色后，支持进一步勾选该角色下的生效星级（1-10星）。
        *   **逻辑组合**: 提供“与/或”开关，用于定义“时间维度”和“用户维度”两个条件集的组合逻辑。
    4.  **后端验证**: 统一的`executePrompt` API网关会实时验证当前调用是否满足场景配置中的所有时间、用户和逻辑规则，实现AI行为的动态切换。


---

## **4. 非功能性需求**

*   **5.1. 性能 (Performance)**: 核心页面FCP < 2.5秒。AI响应时间平均 < 5秒。
*   **5.2. 安全性 (Security)**: 数据库写操作需进行用户角色和所有权校验。提示词内容通过`promptKey`机制受保护。
*   **5.3. 可用性与可访问性 (Usability & Accessibility)**: UI应在主流设备尺寸上表现良好。所有交互元素都应有清晰的标签和状态。

---

## **5. 未来路线图 (Future Roadmap)**

*   **V2.3: 交易与沟通闭环深化**
    *   在`ChatDialog`中引入文件和图片传输功能。
    *   引入平台担保交易和订单管理系统。
*   **V2.4: AI能力增强**
    *   利用`executePrompt`网关，实现基于提示词优先级的智能路由和故障切换。
    *   引入多模态AI，支持视频、音频作为输入。
*   **V2.5: 管理后台功能扩展**
    *   在`/admin-dashboard`中增加“用户行为分析”、“数据看板”等模块。

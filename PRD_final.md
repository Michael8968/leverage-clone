
# **AI 智能匹配与创意生成平台 - 产品需求文档 (PRD)**

**版本**: 2.0
**日期**: 2024年8月5日
**状态**: 进行中

---

## **1. 愿景与战略**

### **1.1. 产品愿景**
构建一个以AI为核心的智能化商业生态系统，无缝连接用户的个性化、模糊化需求与高品质、可定制的产品及服务。我们致力于成为创意和需求的最佳“翻译官”和“匹配者”，让每一次发现都充满惊喜，让每一次交易都精准高效。

### **1.2. 核心问题**
*   **用户侧**: 消费者常常“只可意会，不可言传”，他们有模糊的想法（比如“想要一个赛博朋克风的礼物”），但难以在传统电商平台通过关键词找到满意的、独特的商品。
*   **供给侧**: 大量的优质供应商和独立设计师，其独特的产品和服务难以精准触达有潜在需求的客户，获客成本高，市场信息不对称。

### **13. 战略目标 (Success Metrics)**
*   **提升匹配成功率**: AI推荐的商品首轮点击率 > 40%。
*   **促进供需撮合**: “需求池”中的需求在72小时内的响应率 > 60%。
*   **激活创意生态**: “创意者工作台”月活跃创作者数量持续增长，每月产出并被采纳的3D模型 > 100个。
*   **平台数据增值**: 知识库条目月增长率 > 10%，形成数据飞轮效应。

---

## **2. 用户画像 (Personas)**

#### **2.1. 管理员 - 李明 (Admin)**
*   **背景**: 平台运营总监，技术背景，追求效率和数据驱动。
*   **目标**: 保证平台AI模型的精准度和数据质量；监控平台健康度；高效管理所有用户和资源。
*   **痛点**: AI推荐效果波动大，难以归因；各模块数据分散，难以统一管理；用户权限分配和管理操作繁琐。
*   **核心诉求**: 一个强大、中心化的后台，能够精细化控制AI行为（提示词）、管理核心数据（知识库、供应商），并对用户进行生命周期管理。

#### **2.2. 供应商 - 创新科技 (Supplier)**
*   **背景**: 一家专注于智能家居产品的科技公司，拥有高质量的产品线但线上推广渠道有限。
*   **目标**: 寻找新的、精准的销售渠道；降低获客成本；接收定制化订单以提升品牌价值。
*   **痛点**: 传统广告投放费用高昂且效果不佳；无法触达有模糊购买意图的潜在高端客户。
*   **核心诉求**: 一个能“听懂”客户潜在需求的平台，将订单直接推送到面前；一个简单易用的后台来管理自己的产品信息。

#### **2.3. 创意者 - 王芳 (Creator)**
*   **背景**: 自由职业3D设计师，对新潮事物充满热情，希望将创意变现。
*   **目标**: 找到能发挥自己设计才能的兼职项目；使用高效的工具进行创作；建立个人品牌，吸引长期客户。
*   **痛点**: 独立接单不稳定，议价能力弱；创作灵感枯竭，需要AI工具辅助。
*   **核心诉求**: 一个能稳定提供设计任务的“任务市场”；一个能将文本描述快速转化为3D模型参考的AI创作工具。

#### **2.4. 普通用户 - 张伟 (User)**
*   **背景**: 追求生活品质的都市白领，喜欢为朋友和自己挑选独特的礼物。
*   **目标**: 快速找到符合自己模糊想法（如“有未来感的台灯”）的商品；能够进行高端、个性化的定制。
*   **痛点**: 传统电商搜索无法满足其感性、模糊的搜索需求；找不到能够提供一对一设计服务的设计师。
*   **核心诉uto**: 一个能理解自然语言甚至图片的“导购AI”；一个能直接与设计师沟通并预约的渠道。

---

## **3. 功能规格 (Functional Specifications)**

### **3.1. 主页: AI 购物助手 (`/`)**

*   **用户故事**:
    *   作为一个**普通用户**，我希望能通过输入文字或上传图片来描述我的想法，以便平台能理解我的需求并推荐相关商品。
    *   作为一个**创意者**，我希望在主页上有一个快速入口，能直接跳转到我的工作台。
*   **核心功能**:
    1.  **智能聊天界面**:
        *   **[UI]** 界面中心为一个响应式的文本输入区域和图片上传按钮。
        *   **[AI]** 支持文本和图片（Data URI格式）混合输入。
        *   **[Flow]** 用户提交后，调用 `getRecommendationsFromText` Action。该Action内部会依次调用 `generateUserProfile` (生成用户画像) 和 `getRecommendations` (获取推荐结果) 两个核心AI流程。
    2.  **AI分析与响应**:
        *   **用户画像卡片**: 在推荐结果前，展示一个由 `generateUserProfile` 生成的用户画像卡片，包含“画像总结”和“关键词标签”。
        *   **商品推荐卡片 (`ProductCard`)**:
            *   **[Data]** 展示由 `recommendProductsOrServices` 流程返回的商品列表（3-5个）。
            *   **[UI]** 卡片包含商品图片（`panoramicImage`）、名称、描述和价格。
            *   **[Action]** “立即购买”按钮链接到商品的 `purchaseUrl` (新窗口打开)。“查看详情”按钮当前功能同上，为未来预留。
    3.  **侧边栏与快捷入口**:
        *   **[UI]** 页面右侧或聊天界面下方，为不同角色提供快捷操作按钮。
        *   **[Role-Based]** “高端定制服务” (所有用户可见，链接到 `/designers`)。“发布到需求池” (普通用户在获得推荐后可见，链接到 `/demand-pool`)。“创意者工作台” (仅创意者角色可见，链接到 `/creator-workbench`)。
*   **验收标准**:
    *   ✅ 用户可以仅输入文本，并获得基于文本的用户画像和商品推荐。
    *   ✅ 用户可以仅上传图片，并获得基于图片的用户画像和商品推荐。
    *   ✅ 用户可以同时输入文本和图片，AI应综合两者信息进行推荐（例如：图片是一条裙子，文字是“换成蓝色”）。
    *   ✅ 返回的商品卡片信息（图片、价格、描述等）必须与知识库中的数据一致。
    *   ✅ 游客（未登录用户）可以与AI交互，但在点击“购买”或“预约”等操作时，应提示登录。

### **3.2. 需求池 (`/demand-pool`)**

*   **用户故事**:
    *   作为一个**普通用户**，当AI推荐不能满足我时，我希望能发布一个公开的需求，让供应商和设计师来找我。
    *   作为一个**供应商/创意者**，我希望浏览一个公开的需求列表，并对我感兴趣的需求进行“抢单”。
*   **核心功能**:
    1.  **需求列表与筛选**:
        *   **[API]** 页面加载时，调用 `GET /api/demands` 获取所有需求数据。
        *   **[UI]** 以表格形式展示需求，包含标题、预算、类别、状态、发布日期等。
        *   **[Filter]** 提供基于“类别”和“状态”的多选筛选功能，并提供一个关键词输入框以模糊搜索“标题”和“标签”。
    2.  **发布新需求**:
        *   **[Role-Based]** 仅“普通用户”可见“发布新需求”按钮。
        *   **[UI]** 点击后弹出 `DemandFormDialog` 模态框。
        *   **[API]** 表单提交时，调用 `POST /api/demands`，将用户ID和表单数据一同发送。
    3.  **响应需求**:
        *   **[Role-Based]** 仅“供应商”和“创意者”可见操作按钮。
        *   **[UI]** 对“开放中”的需求显示“抢单”按钮。
        *   **[API]** 点击“抢单”后，调用 `POST /api/demands/{id}/respond`，将自己的用户ID作为 `supplier_id` 发送。后端应将需求状态更新为“洽谈中”。
*   **验收标准**:
    *   ✅ 需求列表能正确显示所有公开需求，并且筛选和搜索功能运作正常。
    *   ✅ 不同角色的用户看到的操作按钮应严格符合其权限。
    *   ✅ 用户成功发布新需求后，该需求应立即出现在列表顶部。
    *   ✅ 供应商/创意者“抢单”后，该需求的状态应在列表上实时更新为“洽谈中”，且“抢单”按钮变为不可用的“开始沟通”按钮。

### **3.3. 创意者工作台 (`/creator-workbench`)**

*   **用户故事**:
    *   作为一个**创意者**，我希望能在一个地方看到所有我可以接的任务、使用AI工具进行创作，并管理我提交的作品。
*   **核心功能**:
    1.  **访问控制**: 页面加载时，检查用户角色。如果不是 `creator`，则显示一个“访问受限”的提示页面。
    2.  **任务与需求 (Tab)**:
        *   **[API]** 调用 `GET /api/demands`，并筛选出状态为“开放中”的需求作为可接受任务。
        *   **[Action]** 提供“接受任务”按钮，其功能与需求池的“抢单”完全一致。
    3.  **3D AI 创作 (Tab)**:
        *   **[UI]** 提供一个文本输入框（Prompt）和一个“生成”按钮。
        *   **[AI]** 点击按钮后，调用 `generate3dModel` Genkit流程，并将返回的图片（Data URI）展示在预览区域。
        *   **[UX]** 在AI生成期间，按钮应处于加载状态，并显示提示信息。
    4.  **我的提交 (Tab)**:
        *   **[API]** 调用 `GET /api/knowledge-base`，并可以根据 `creator_id` (未来需在schema中添加) 进行筛选。
        *   **[UI]** 以表格形式展示自己提交并被采纳的作品（当前简化为展示所有知识库条目）。
*   **验收标准**:
    *   ✅ 非创意者角色访问此页面时，会被友好地拒绝。
    *   ✅ “任务与需求”列表能正确加载平台上的公开需求。
    *   ✅ 用户输入文本后，3D创作工具能成功调用AI并展示返回的图片。
    *   ✅ “我的提交”列表能正确加载并展示知识库中的条目。

---

## **4. 后台管理模块 (Admin-Only)**

### **4.1. 供应商中心 (`/suppliers`)**

*   **用户故事**:
    *   作为一个**管理员**，我需要一个批量处理工具，能通过上传CSV文件，由AI辅助快速导入大量供应商数据。
*   **核心功能**:
    1.  **批量处理 (Tab)**:
        *   **[UI]** 提供一个文件上传区域（支持拖拽），以及“下载模板”和“导出数据”按钮。
        *   **[Flow]** 上传CSV文件后，调用 `handleFileUpload` Action。该Action内部会读取文件内容并调用 `processSupplierData` Genkit流程。
        *   **[AI]** `processSupplierData` 流程负责解析CSV，并根据预设规则（如行业相关性）为每条数据评估一个“匹配度”分数。
        *   **[UI]** AI处理完成后，结果以表格形式展示在“处理结果”区域，包含名称、类别和AI评定的匹配度。
*   **验收标准**:
    *   ✅ 管理员可以下载标准的CSV模板文件。
    *   ✅ 上传格式正确的CSV文件后，AI能成功处理并返回带匹配度的供应商列表。
    *   ✅ 处理结果可以被成功导出为新的CSV文件。

*(其他后台管理模块，如知识库、公共资源、提示词和权限管理，其核心功能在现有代码中已相对明确，此处不再赘述，但均应遵循类似的 User Story -> Features -> Acceptance Criteria 的规范进行细化。)*

---

## **5. 非功能性需求**

*   **5.1. 性能 (Performance)**
    *   **指标**: 核心页面的首次内容绘制时间 (FCP) < 2.5秒。AI响应时间（从用户提交到返回结果）平均 < 5秒。
    *   **实现**: Next.js App Router, 服务端组件 (RSC), 图片优化 (next/image)。
*   **5.2. 安全性 (Security)**
    *   **策略**: 所有API的写操作（POST, PUT, DELETE）都应进行用户角色和权限校验。严格遵守最小权限原则。
    *   **实现**: 在Express后端的所有敏感路由上添加认证中间件。
*   **5.3. 可用性与可访问性 (Usability & Accessibility)**
    *   **策略**: 所有交互元素（按钮、输入框）都应有清晰的`aria-label`。UI应在主流设备尺寸（手机、平板、桌面）上表现良好。
    *   **实现**: 使用ShadCN组件，遵循其可访问性设计。采用响应式布局（Tailwind CSS）。

---

## **6. 未来路线图 (Future Roadmap)**

*   **V2.1: 交易与沟通闭环**
    *   在需求池中集成即时沟通（IM）功能，让供需双方可以直接对话。
    *   引入平台担保交易和订单管理系统。
*   **V2.2: 创作者生态深化**
    *   在知识库中增加 `creator_id` 字段，实现创作者作品的真正归属。
    *   为创意者提供作品集页面，建立个人品牌。
*   **V2.3: AI能力增强**
    *   引入多模态AI，支持视频、音频作为输入。
    *   利用AI对用户行为数据进行分析，实现更精准的个性化推荐和需求预测。

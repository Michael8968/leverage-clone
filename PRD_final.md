# **AI 智能匹配与创意生成平台 - 产品需求文档 (PRD)**

**版本**: 2.0 (Final)
**日期**: 2024年8月6日
**状态**: 已完成

---

## **1. 愿景与战略**

### **1.1. 产品愿景**
构建一个以AI为核心的智能化商业生态系统，无缝连接用户的个性化、模糊化需求与高品质、可定制的产品及服务。我们致力于成为创意和需求的最佳“翻译官”和“匹配者”，让每一次发现都充满惊喜，让每一次交易都精准高效。

### **1.2. 核心问题**
*   **用户侧**: 消费者常常“只可意会，不可言传”，他们有模糊的想法（比如“想要一个赛博朋克风的礼物”），但难以在传统电商平台通过关键词找到满意的、独特的商品。
*   **供给侧**: 大量的优质供应商和独立设计师，其独特的产品和服务难以精准触达有潜在需求的客户，获客成本高，市场信息不对称。

### **1.3. 战略目标 (Success Metrics)**
*   **提升匹配成功率**: AI推荐的商品首轮点击率 > 40%。
*   **促进供需撮合**: “需求池”中的需求在72小时内的响应率 > 60%。
*   **激活创意生态**: “创意者工作台”月活跃创作者数量持续增长，每月产出并被采纳的3D模型 > 100个。
*   **平台数据增值**: 知识库（产品/服务）条目月增长率 > 10%，形成数据飞轮效应。

---

## **2. 用户画像 (Personas)**

#### **2.1. 管理员 - 李明 (Admin)**
*   **背景**: 平台运营总监，技术背景，追求效率和数据驱动。
*   **目标**: 保证平台AI模型的精准度和数据质量；监控平台健康度；高效管理所有用户和资源。
*   **痛点**: AI推荐效果波动大，难以归因；各模块数据分散，难以统一管理；用户权限分配和管理操作繁琐。
*   **核心诉求**: 一个强大、中心化的后台，能够精细化控制AI行为（提示词）、管理核心数据（产品、需求），并对用户进行生命周期管理。

#### **2.2. 供应商 - 创新科技 (Supplier)**
*   **背景**: 一家专注于智能家居产品的科技公司，拥有高质量的产品线但线上推广渠道有限。
*   **目标**: 寻找新的、精准的销售渠道；降低获客成本；接收定制化订单以提升品牌价值。
*   **痛点**: 传统广告投放费用高昂且效果不佳；无法触达有模糊购买意图的潜在高端客户。
*   **核心诉诉求**: 一个能“听懂”客户潜在需求的平台，将订单直接推送到面前；一个简单易用的后台来管理自己的产品和服务信息，并能实时更新到数据库。

#### **2.3. 创意者 - 王芳 (Creator)**
*   **背景**: 自由职业3D设计师，对新潮事物充满热情，希望将创意变现。
*   **目标**: 找到能发挥自己设计才能的兼职项目；使用高效的工具进行创作；建立个人品牌，吸引长期客户。
*   **痛点**: 独立接单不稳定，议价能力弱；创作灵感枯竭，需要AI工具辅助。
*   **核心诉求**: 一个能稳定提供真实设计任务的“任务市场”；一个能将文本描述快速转化为3D模型参考的AI创作工具。

#### **2.4. 普通用户 - 张伟 (User)**
*   **背景**: 追求生活品质的都市白领，喜欢为朋友和自己挑选独特的礼物。
*   **目标**: 快速找到符合自己模糊想法（如“有未来感的台灯”）的商品；能够进行高端、个性化的定制。
*   **痛点**: 传统电商搜索无法满足其感性、模糊的搜索需求；找不到能够提供一对一设计服务的设计师。
*   **核心诉求**: 一个能理解自然语言甚至图片的“导购AI”；一个在AI推荐不满意时，能将需求公开发布的渠道。

---

## **3. 功能规格 (Functional Specifications)**

### **3.1. 主页: AI 购物助手 (`/`)**

*   **用户故事**:
    *   作为一个**普通用户**，我希望能通过输入文字或上传图片来描述我的想法，平台能理解我的需求并从**真实的产品库**中推荐相关商品。
    *   作为一个**普通用户**，当AI推荐不合我意时，我希望能一键将我的需求发布到“需求池”。
*   **核心功能**:
    1.  **数据加载**: 页面加载时，从Firestore `products`集合中获取所有产品数据，供后续AI流程使用。
    2.  **智能聊天界面**:
        *   **[UI]** 界面中心为一个响应式的文本输入区域和图片上传按钮。
        *   **[AI]** 支持文本和图片（Data URI格式）混合输入。
        *   **[Flow]** 用户提交后，依次调用 `generateUserProfile` (生成用户画像) 和 `getProductRecommendations` (获取推荐结果) 两个核心AI流程。`getProductRecommendations`会接收从数据库加载的真实产品列表作为参数。
    3.  **AI分析与响应**:
        *   **用户画像卡片**: 在推荐结果前，展示一个由 `generateUserProfile` 生成的用户画像卡片，包含“画像总结”和“关键词标签”。
        *   **商品推荐卡片 (`ProductCard`)**:
            *   **[Data]** 展示由 `getProductRecommendations` 流程返回ID、并从前端产品列表中匹配出的**真实商品列表**（3-5个）。
            *   **[UI]** 卡片包含商品图片、名称、描述和价格。
            *   **[Action]** “立即购买”按钮会尝试打开商品的 `purchaseUrl` (新窗口)。“查看详情”按钮为交互预留。
    4.  **快捷操作入口**:
        *   **高端定制 (`CustomServiceConnector`)**: 所有用户可见。点击后会引导用户至`/suppliers`页面，寻找供应商。
        *   **发布到需求池 (`DemandPoolConnector`)**: **仅`user`角色可见**，且在AI给出推荐后出现。点击后引导用户至`/demand-pool`页面。
*   **验收标准**:
    *   ✅ 用户可以仅输入文本，并获得基于文本的用户画像和**真实商品**推荐。
    *   ✅ 用户可以同时输入文本和图片，AI应综合两者信息进行推荐。
    *   ✅ 返回的商品卡片信息（图片、价格、描述等）必须与`products`集合中的数据一致，并包含可交互的购买按钮。
    *   ✅ “发布到需求池”按钮的出现时机和角色权限符合规定。
    *   ✅ “高端定制”按钮能正确跳转到供应商页面。

### **3.2. 需求池 (`/demand-pool`)**

*   **用户故事**:
    *   作为一个**管理员**，我希望能为公开的需求，调用AI从真实的产品/供应商库中寻找最佳匹配。
*   **核心功能**:
    1.  **需求列表加载**:
        *   **[Data]** 页面加载时，从Firestore `demands`集合获取所有需求数据并在表格中展示。
    2.  **AI匹配推荐 (管理员功能)**:
        *   **[Action]** 管理员选中一个或多个需求后，点击“AI推荐”按钮。
        *   **[Data]** 系统会从Firestore `products`集合中获取所有产品数据，作为可供匹配的“创意方”资源。
        *   **[Flow]** 调用`recommendCreatives` AI流程，将选中的需求和**真实的产品数据**一同发送给AI进行分析。
        *   **[UI]** 在弹窗中展示AI返回的匹配建议和理由。
*   **验收标准**:
    *   ✅ 需求列表能正确显示所有来自Firestore的需求。
    *   ✅ 管理员启动AI推荐时，系统能正确加载`products`集合的数据作为匹配源。
    *   ✅ AI推荐结果能正确展示在弹窗中。

### **3.3. 创意者工作台 (`/creator-workbench`)**

*   **用户故事**:
    *   作为一个**创意者**，我希望能在一个地方看到所有我可以接的真实任务，并使用多个AI工具进行创作，还能方便地查看我的提交历史。
*   **核心功能**:
    1.  **访问控制**: 页面加载时，检查用户角色。如果不是 `creator`，则显示一个“访问受限”的提示页面。
    2.  **任务与需求 (Tab)**:
        *   **[Data]** 通过Firestore查询，加载`demands`集合中所有`status`为“开放中”的**真实需求**。
        *   **[UI]** 使用`DemandList`组件将这些需求以列表形式展示。创作者可以点击“接受任务”来承接需求。
    3.  **AI 创作 (Tab)**:
        *   **[UI]** 此模块为一个创作中心，包含三个子标签页，分别对应不同的AI工具。
        *   **子标签页 1: 内置模型**:
            *   **[UI]** 提供一个文本输入框（Prompt）和一个“生成”按钮。
            *   **[Flow]** 调用 `generate3dModel` Genkit流程，快速生成2D预览图。
        *   **子标签页 2: Tripo3D**:
            *   **[UI]** 提供文本输入框和API Key输入框（支持个人Key和平台全局Key）。
            *   **[Flow]** 调用 `generateTripo3dModel` 创建任务，然后通过 `getTripo3dModelStatus` 轮询状态。
            *   **[UI]** 实时显示任务进度（百分比）和状态信息。
        *   **子标签页 3: Gemini Image (Nano-Banana)**:
            *   **[UI]** 提供文本输入框和一个图片上传区域。
            *   **[Flow]** 调用 `generateNanoBananaImage` 流程，支持图生图编辑或文生图创作。
        *   **统一提交流程**:
            *   **[UI]** 无论使用哪个工具，成功生成图片后，下方都会显示一个统一的`SubmissionForm`表单。
            *   **[Data]** 创作者填写作品名称、价格、描述等信息，点击“提交审核”后，调用`addDoc`将此作品作为一条新记录（包含`creatorId`和`status: '审核中'`）**存入`products`集合**。
    4.  **我的提交 (Tab)**:
        *   **[Data]** 功能已完善。通过Firestore查询，实时加载并展示当前登录的`creator`在`products`集合中提交的所有作品。
        *   **[UI]** 以表格形式清晰展示作品的预览图、名称、价格、提交时间以及当前的审核状态（如 `审核中`, `已入库`, `需要修改`）。
*   **验收标准**:
    *   ✅ 非创意者角色访问此页面时，会被友好地拒绝。
    *   ✅ “任务与需求”列表能正确加载并展示平台上的真实公开需求。
    *   ✅ “AI 创作”中的三个子工具（内置、Tripo3D、Gemini Image）均可成功调用对应的AI流程并展示返回的图片。
    *   ✅ AI生成图片后，创作者可以通过表单成功提交作品，数据被正确写入`products`集合。
    *   ✅ “我的提交”标签页能正确加载并展示当前创作者的所有提交历史和审核状态，不再是静态占位符。

---

## **4. 后台管理模块 (Admin/Supplier-Only)**

### **4.1. 供应商中心 (`/suppliers`)**

*   **用户故事**:
    *   作为一个**供应商**，我需要一个后台来管理我的公司信息和所有产品，所有更改都应**实时保存到数据库**。
    *   作为一个**供应商**，我需要一个批量处理工具，能通过上传CSV文件，由AI辅助快速分析潜在客户数据并存入数据库。
*   **核心功能**:
    1.  **产品管理**:
        *   **[Data]** 页面加载时，从Firestore `products`集合加载该供应商的全部产品。
        *   **[Action]** 提供“添加”、“更新”、“删除”功能，这些操作会直接调用Firestore API (`addDoc`, `updateDoc`, `deleteDoc`)，实时修改数据库中的`products`集合。
        *   **[Data]** 产品的补充字段（规格参数）与产品信息一同保存在`products`集合的文档中，实现了完全的持久化。
    2.  **批量数据处理 (`DataProcessor`)**:
        *   **[Flow]** 上传CSV文件后，调用 `evaluateSellerData` Genkit流程。
        *   **[Data]** AI处理完成后，用户点击“保存到数据库”按钮，将返回的分析结果（包含名称、类别、匹配度等）以`writeBatch`的方式**批量写入Firestore的`suppliers`集合**。
*   **验收标准**:
    *   ✅ 供应商可以添加、编辑、删除产品，刷新页面后所有更改都应保留。
    *   ✅ 产品的补充字段也能被成功保存和读取。
    *   ✅ 上传CSV文件后，AI能成功处理，并且处理结果可以被成功保存到Firestore。

### **4.2. 其他后台模块 (Admin-Only)**

*   **权限管理 (`/permissions`)**:
    *   **[UI]** 提供一个用户列表，展示用户名、邮箱、角色、状态和评级。
    *   **[Data]** 页面加载时，从Firestore `users`集合获取所有用户数据，作为真实数据源。
    *   **[Action]** 提供用于更改角色和执行用户操作的下拉菜单，作为未来功能的交互占位符。
*   **提示词管理 (`/prompt-management`)**:
    *   **[UI]** 提供一个提示词列表，展示提示词名称、ID、功能、生效范围和状态。
    *   **[Data]** **使用静态模拟数据**，展示不同业务场景的提示词，作为高保真原型。
*   **知识库管理 (`/knowledge-base`)**:
    *   **[UI]** 提供一个知识条目列表。
    *   **[Data]** 页面加载时，从Firestore `products`集合获取所有产品数据，作为知识库的真实数据源。
*   **公共资源库 (`/public-resources`)**:
     *   **[UI]** 提供一个API接口和外部链接的管理列表。
     *   **[Data]** **使用静态模拟数据**，展示API端点、认证方式等信息，作为高保真原型。

---

## **5. 非功能性需求**

*   **5.1. 性能 (Performance)**
    *   **指标**: 核心页面的首次内容绘制时间 (FCP) < 2.5秒。AI响应时间（从用户提交到返回结果）平均 < 5秒。
    *   **实现**: Next.js App Router, 服务端组件 (RSC), 图片优化 (next/image), Firestore数据加载状态管理（骨架屏）。
*   **5.2. 安全性 (Security)**
    *   **策略**: 所有数据库写操作都应在未来进行用户角色和所有权校验（例如，供应商只能修改自己的产品）。
    *   **实现**: （当前）前端基于角色进行UI显隐控制。
*   **5.3. 可用性与可访问性 (Usability & Accessibility)**
    *   **策略**: 所有交互元素都应有清晰的标签和状态。UI应在主流设备尺寸（手机、平板、桌面）上表现良好。
    *   **实现**: 使用ShadCN组件，遵循其可访问性设计。采用响应式布局（Tailwind CSS）。

---

## **6. 未来路线图 (Future Roadmap)**

*   **V2.1: 交易与沟通闭环**
    *   在需求池中集成即时沟通（IM）功能，让供需双方可以直接对话。
    *   引入平台担保交易和订单管理系统。
*   **V2.2: 创作者生态深化**
    *   在`products`集合中增加 `creator_id` 字段，实现创作者作品的真正归属。
    *   为创意者提供作品集页面，建立个人品牌，并完善“我的提交”功能。
*   **V2.3: AI能力增强**
    *   引入多模态AI，支持视频、音频作为输入。
    *   利用AI对用户行为数据进行分析，实现更精准的个性化推荐和需求预测。
